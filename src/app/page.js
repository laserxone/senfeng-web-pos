import POS from "@/components/POS";

export default function Home() {
 return (
   //  <POS />

   <div style={{width:'100vw', height:'100vh', display:'flex', alignItems:'center', justifyContent:'center'}}>
      <p>Site not accessible</p>
   </div>
 )
}
